$(window).resize(function() {
	var totalHeight = $(window).height() * .9;
	$('.navigation-container').css('height', totalHeight);
});

var totalHeight = $(window).height() * .9;
$('.navigation-container').css('height', totalHeight);


